# `/codebuddy-hud:setup`

Configure CodeBuddy Code to use CodeBuddy HUD as the terminal `statusLine` command.

Equivalent shell command:

```bash
node bin/codebuddy-hud.js setup
```

The command preserves the previous `statusLine` in `.meta.json` so `uninstall` can restore it.
